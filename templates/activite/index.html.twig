{% extends 'base.html.twig' %}

{% block title %}Hello ActiviteController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Hello {{ controller_name }}! ✅</h1>
    <div>
        <a href="{{ path('add-form_activite') }}">
            <button>
                {{ 'add_an_activity' | trans }}
            </button>
        </a>
        <p>
            {{ 'hello there is a virus on your computer' | trans }}
        </p>
        <a href="{{ path('activite') }}">
            <button>
                Traduire
            </button>

    </div>

    <ul>
            {% for activite in activites %}
                <li>
                    {{ activite.name }}
                    <img src="{{ activite.image }}" alt="">
                   <div>
                       <a href="{{ path('update_activite', {'activite' : activite.id}) }}">
                           <button>
                               Update
                           </button>
                       </a>
                   </div>
                    <div>
                        <a href="{{ path('delete_activite', {'activite' : activite.id}) }}">
                            <button>
                                Delete
                            </button>
                        </a></div>
                </li>
            {% endfor %}
        </ul>


    This friendly message is coming from:
    <ul>
        <li>Your controller at <code><a href="{{ 'C:/Users/HB/agenceVoyage/src/Controller/ActiviteController.php'|file_link(0) }}">src/Controller/ActiviteController.php</a></code></li>
        <li>Your template at <code><a href="{{ 'C:/Users/HB/agenceVoyage/templates/activite/index.html.twig'|file_link(0) }}">templates/activite/index.html.twig</a></code></li>
    </ul>
</div>
{% endblock %}
